# requirements.txt — 开发环境依赖
# Runtime metadata is authoritative in pyproject.toml; this file is only for
# contributor environments and must not be used as distribution metadata.

# Testing
pytest>=6.0
pytest-xdist>=2.0
pytest-cov>=2.10
pytest-benchmark>=3.2
pytest-sugar>=0.9
parameterized

# Lint & Type Check
ruff>=0.4.0
mypy>=1.5

# Viz (for tearsheets)
matplotlib>=3.3
seaborn>=0.11
ipython>=7.0
statsmodels>=0.14

# Bayesian (optional)
# pymc>=5.0

# Data (optional)
pandas-datareader>=0.8.0
akshare

# Dev tools
setuptools
packaging
scikit-learn
