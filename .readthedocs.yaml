# Read the Docs configuration
# See https://docs.readthedocs.io/en/stable/config-file/v2.html
version: 2
build:
  os: ubuntu-22.04
  tools:
    python: "3.12"
  jobs:
    post_checkout:
      - pip install -e ".[viz]"
# MkDocs config must be top-level (required as of 2025)
mkdocs:
  configuration: mkdocs.yml
