# SDD ledger — plan: docs/plans/2026-08-12-fincore-empyrical-pyfolio-convergence.md

- Controller: /root
- Branch: `codex/fincore-convergence-alphalens`
- Plan baseline commit: `257b7fe`
- Task 1: complete (commits `60a1327`, `53af921`, `e858725`; final review CLEAN; focused `35 passed`; trusted baseline `2279 passed, 14 skipped`; branch coverage `94.0%`)
- Task 2: complete (commits `99a57df`, `d277ea6`, `a569315`; final review CLEAN; manifest suite `25 passed`; 54/49 empyrical and 11-workflow pyfolio targets frozen)
- Task 3: complete (commits `a5302f4`, `404f80b`; final review CLEAN; focused `203 passed`; broad `1349 passed`; manifest `25 passed`; C0 `54/54`, C1 `49/49`)
- Task 4: complete (commits `cc12cb7`, `4e0824d`, `5f7529a`, `816e128`; final review CLEAN; convenience/frozen-value RED `19 failed, 187 passed`; follow-up focused `206 passed`; enhanced-binary matrix `151 passed`; context impact `291 passed, 3 pinned warnings`; isolated broad gate `1297 passed, 3 pinned warnings`; manifest `26 passed`; Task 3 regression `203 passed`; Task 5 selector regression `159 passed`)
- Task 5: complete (commits `575a040`, `d686b11`, `fd03bf6`; final review CLEAN; first follow-up RED `17 failed, 10 passed` plus generated-fixture RED; first focused matrix `28 passed`; volume P1 RED `13 failed, 2 passed`; volume P1 focused `15 passed`; expanded positions `88 passed`; manifest + domain `159 passed`; Task 3 regression `203 passed`)
- Task 6: complete (commits `5863321`, `cd01847`, `cbbeecd`, `5a8a90a`; final review CLEAN; initial assertion RED `4 failed, 9 passed`; public/lazy/drawdown RED `26 failed, 2 passed`; perf/full/no-write RED `7 failed`; stored-state RED `2 failed`; common-utils import RED `1 failed`; three review cycles closed index/DST/dependency boundaries; final focused `151 passed`; Task 6 impact `893 passed, 9 expected business warnings`; fresh Task 7 regression selector `354 passed, 9 expected business warnings`; moved-class audit `69/69` methods)
- Task 7: complete (implementation commits `39be970` + P1 fix `eea7fb4`; final fix round `03d4e70`; review CLEAN — spec ✅, quality approved). Implementation: initial contract RED `151 failed, 3 passed`; dependency selector `1625 passed, 3 pinned warnings`; schema/contracts `232 passed`; exit `351 passed`; review P1 strict Pyfolio raw-boundary RED `2 failed`, GREEN `2 passed, 1 expected warning`; focused workflow/public `38 passed, 1 expected warning`; contracts + strict Empyrical `435 passed`; fresh Task 6 exact `356 passed, 10 expected business warnings`; scoped mypy/ruff/format/diff clean. Fix round `03d4e70` closed: information_ratio enhanced validation on metrics surface, complete dispatch coverage for 20 shared enhanced exports (`__fincore_dispatch_spec__` markers + registry keys), lazy kernel refresh after module reload (flat + bound class), empty-rows ValidationError for positions/transactions/market_data schemas and context/metric-argument boundaries. Gates: contract files `231 passed`; contracts+core+report `376 passed`; compat `622 passed` (controller-verified).
- Task 7: minor (deferred): `fincore/_dispatch.py:238-248` — `__doc__`/`__annotations__`/`__signature__`/`__module__` snapshot the initial kernel and never refresh after reload (introspection-only staleness); `fincore/_dispatch.py:257-263` — `_current_contract` mutates `_kernel`/`_signature` unsynchronized (benign race, each invocation uses a locally resolved pair); task-7-report fix-round arithmetic omits 3 tests added mid-round (cosmetic).
- Pre-existing whole-suite failures (verified identical at `eea7fb4` baseline, NOT caused by Task 7 fix round): 18 failures in `tests/test_empyrical` (NaN-on-empty/instance-state assertions conflicting with enhanced validation; e.g. `test_missing_coverage.py::test_instance_with_returns_creates_context`, `test_perf_attrib.py::test_perf_attrib_regression`, empty-returns-NaN family) plus ~22 `test_edge_cases.py`/line-coverage failures reported by the implementer. TRIAGE REQUIRED before Task 12's full-suite release gate — these tests assert pre-Task-3/7 behavior; migrate to new contracts or fix fixtures, never weaken.
- Task 12 ledger: update three stale instance-call assertions in `tests/integration/test_workflows.py` to the Task 3 stored-state positional-binding contract before the offline integration release gate.
- Status: Tasks 1–7 complete and CLEAN; Tasks 8–13 pending. Pre-existing suite failures (above) must be triaged before the Task 12 release gate.
- Task 8: complete (commits `5392217`, `769fd5b`, fix round `4ce3a01`; review spec ✅ quality approved; fix round 1 ADDRESSED, no new breakage). RED was 6 fails + 1 collection error; Task 8 gate `tests/test_report tests/test_pyfolio tests/test_tearsheets tests/test_utils` 326 passed pre-fix / 327 passed post-fix; compat+core+viz+contracts `1003 passed`; ruff/format/diff-check clean; scoped mypy Success; wheel build includes echarts asset + model module. Vendored ECharts 5.5.0 (`fincore/report/assets/echarts.min.js`, 1,029,203 B, inline-safe, no CDN refs). `print_table(export=ExportConfig(output_dir=...))` contract verified; `matplotlib.use()` fully removed from import paths; PDF temp cleanup on all failure paths.
- Task 8: minor (deferred): `fincore/report/model.py:335-337` — `ReportModel.title` set but unused by renderers (dead metadata); `fincore/report/artifacts.py:34-38` — `closed` returns True for figure-less artifacts without release action (misleading semantics; defensible); `tests/test_report/test_offline_report.py` socket monkeypatch is decorative (CDN-marker assertions do the real work); `tests/test_pyfolio/test_backend_side_effect.py:902,918` — `finally: matplotlib.use("svg")` resets to svg instead of restoring prior backend.
- Task 8: parked — enhanced stateful `Pyfolio(returns=...).create_full_tear_sheet(returns)` raises `TypeError: 'float' object does not support item assignment` (pandas `_assign_where` via numpy ufunc `out=`) in `fincore/metrics/returns.py:152 cum_returns` reached through `tearsheets/returns.py:282 plot_rolling_returns` — RULING: real bug, verified PRE-EXISTING at `660c357` (before Task 8), Task 8 did not touch that path; must be fixed before the Task 12 full-suite release gate (enhanced full-sheet main chain).
- Task 8: concern (accepted): inlined ECharts makes each HTML report ~1 MB larger — accepted trade-off for self-contained offline reports.
- Task 9: complete (commit `638a94b`; review spec ✅ quality approved; no fix loop). One `ExtensionRegistry` (DuplicatePolicy OVERWRITE/ERROR/IGNORE, priority, Scope BUILTIN/GLOBAL/LOCAL, RLock, `isolated_registry()` snapshot/restore CM) shared by metrics/viz/hooks; `AnalysisHookContext` rename with PEP 562 deprecation alias (old imports work with DeprecationWarning); `viz.get_backend()` registry-first; `AnalysisContext.plot()/compute()` and `RollingEngine.available_metrics`/`compute()` registry-sourced (Task 10 interface ready). Gates: plugin+hooks+integration `39 passed`; viz+core+contracts+report `438 passed`; full serial sweep `958 passed, 1 failed` (pre-existing, stash-verified). Controller-verified 39 passed.
- Task 9: minor (deferred): `plugin/registry.py:183` — `unregister()` for HOOK pops whole list but returns only last entry (surprising, untested, unused); `hooks/events.py:179` — `list_events()` iterates without the registry lock (unlocked read left); `tests/integration/test_plugin_end_to_end.py:74` — available_metrics assertion tautological if both hardcoded (implementation itself genuinely registry-sourced); `viz/base.py:117` — registered backend classes must be no-arg instantiable (documented); DuplicatePolicy silently not applied to HOOK kind (docstring gap).
- Triage note (controller-verified): `tests/integration/test_workflows.py` now shows 3 failures (`test_workflow_with_empyrical_class`, `test_empyrical_vs_flat_api`, `test_context_vs_empyrical`) — these are exactly the Task 12 ledger item (three stale instance-call assertions to migrate to the Task 3 stored-state positional-binding contract). Owned by Task 12.
- Status: Tasks 1–9 complete and CLEAN; Tasks 10–13 pending. Pre-existing suite failures (18 tests/test_empyrical + cum_returns TypeError + 3 integration stale assertions) all scheduled for triage/fix at Task 12.
- Task 10: complete (commits `ea28af1` + fix round `b46514a`; review found Critical dead fast path → fix round 1 ADDRESSED both findings, re-review PASS, no new breakage). `RollingMoments` shared per window (spy-proven exactly 1 build per batch, needs union {mean,std,downside_rms,cov,var}); engine sortino ≡ canonical `roll_sortino_ratio`; vectorized roll_alpha/roll_alpha_beta (~790x, n=25200 w=63: 4.90s→0.0062s); chunked max_drawdown (w=504 RSS delta 286→32 MiB); engine_all RSS 289→19 MiB; benchmark runners with fresh-subprocess RSS/tracemalloc provenance; compare_benchmarks slope/threshold gates; CI perf job without continue-on-error; round-trip scaling locked (quantity-deque advantage). Gates: test_core + rolling contracts `132 passed`; compat + rolling metrics + benchmark `677 passed`; compat oracle green (allclose equal_nan).
- Task 10: minor (deferred): `scripts/compare_benchmarks.py:112-117` — per-case `rss_unit` presence checked but value not validated (only top-level); `fincore/core/rolling_moments.py:89` — engine moments propagate NaN through NaN-containing windows while canonical per-window metrics nan-skip (pre-existing, unpinned); window=1 edge: engine sortino may yield inf where canonical returns NaN (unpinned).
- Status: Tasks 1–10 complete and CLEAN; Tasks 11–13 pending.
- Task 11: complete (commit `ac02c94`; review spec ✅ quality approved; no fix loop). pyproject single metadata source; `__version__` via importlib.metadata with tested pyproject fallback (loud failure); setup.py metadata-free shim; extras restructured to the brief's 9 functional extras + datareader/viz aliases + dev + explicit `all` union (no self-reference); classifier Beta; echarts asset + report model in wheel; example CSV/XLSX → examples/data/; Pyfolio removed from root `__all__` (star import core-only safe, explicit access → DependencyError naming `pip install fincore[pyfolio]`, guard order verified); check_release_consistency (five-way agreement + no-self-dep); test_installed_wheel (isolated -S -E consumers, target-only sys.path, provenance asserts); CI/publish gates. Evidence: 6/6 installed-wheel profiles, 17/17 packaging tests (controller-verified), 648 compat, twine 2/2.
- Task 11: minor (deferred): `fincore/report/render_pdf.py` raises plain ModuleNotFoundError for missing playwright (no DependencyError gate — follow-up); `fincore/__init__.py:61` `raise exc` instead of bare `raise` (cosmetic); `check_release_consistency.py:533` `_SELF_DEP_RE` misses `fincore>=x`-style self-refs in wheel METADATA (source-level check covers the bracket form); pip --target re-downloads deps per CI run (pymc matrix risk under CI timeout).
- Task 12A: complete (commit `1859574`; review spec ✅ quality approved). Removed `fincore.core.*` lax mypy override; owned-file errors 3 → 0 via 3 semantic-preserving casts (registry `Any` dispatch narrowing, `Series | ndarray` kernel narrowing); full-package 159 → 158 / 30 → 29 files; regression tests/contracts + tests/test_core `345 passed`; ruff/format/diff clean. Residual 96 errors in the literal scoped command are all in later batches' files (handoff inventory in task-12a-report.md); Step 4's full-package gate subsumes.
- Task 12B: complete (commit `1ba421c`). Removed `fincore.metrics.*` lax mypy override; owned-file errors 156 → 0 across 15 files via runtime-neutral casts/narrowing at align boundaries, out-buffer tail restructures, float()/cast scalar pinning, two per-line `[misc]` ignores for pandas-stub label slices, and private-helper annotations (public signatures untouched); full-package 158 → 53 errors (remainder in 12C/12D/12E files — handoff inventory in task-12b-report.md); regression tests/test_metrics + tests/compat `1123 passed, 1 failed` (manifest byte-idempotence failure verified pre-existing on pristine tree; a mid-batch timing-strict-policy regression from np.asanyarray conversion was reverted to a cast); ruff/format/diff/compileall clean.
- Triage note (12F scope): `tests/compat/test_manifest_integrity.py::test_full_generator_is_byte_idempotent_when_pinned_roots_are_available` fails at the fixture-freshness assert (regenerated flat-api fixture bytes differ from committed at index 6065 — source sha256 drift after Task 11's regeneration, now stale again after 12A/12B source edits). Generator idempotence itself (first == second) passes. Resolution for 12F: after all source changes settle (post-12E), regenerate the manifest fixtures once with the explicit-regen-and-review path (plan Task 2 upgrade rule) and commit; do NOT weaken the freshness assertion.
- Task 12B: complete (commits `1ba421c` + fix round `497a693`; review Important → fix round 1 ADDRESSED, re-review PASS). Removed `fincore.metrics.*` lax override; owned errors 156 → 0 across 15 files; full-package 158 → 53; 21 float() pins reverted to `cast("float", ...)` (runtime containers bit-identical to pre-batch); no signature/default changes; 3 per-line `[misc]` ignores in drawdown.py with rationale; regression tests/test_metrics + tests/compat `1123 passed, 1 pre-existing failure` (manifest byte-idempotence fixture-freshness — triaged for 12F); ruff/format/compileall/diff clean.
- Status: 12A-12B complete; 12C-12F pending. Full-package mypy at 53 (all in 12C/12D/12E files: pyfolio 1, viz 11, risk/evt 20, attribution 13, optimization 5, tearsheets 2, simulation 1).
