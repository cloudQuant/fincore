# Minimum supported dependency combination for fincore.
#
# These are the floors declared in pyproject.toml, plus the exclusions for the
# known curl_cffi -> eventlet -> pyOpenSSL incompatibility (PyOpenSSL 24.x
# references the removed GEN_EMAIL constant in newer cryptography releases,
# breaking yfinance and akshare imports).  `scripts/check_dependency_matrix.py`
# and the CI optional-extra smoke job resolve against this file.

numpy>=1.24.0
pandas>=1.5.0
scipy>=1.3.0
pytz>=2023.3
packaging>=21.0

# data-yahoo / data-cn transitives: keep a working TLS stack.
pyOpenSSL>=26.4.0
cryptography<51.0.0
