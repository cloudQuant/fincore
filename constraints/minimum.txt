# Minimum supported dependency combination for fincore (Python 3.11).
#
# Unlike latest.txt (which lets pip choose), this file pins the *oldest
# resolvable* exact combination: each version is the earliest release with a
# Python 3.11 wheel for its line, so ``pip install -c minimum.txt fincore`` is
# an actual isolation install rather than a floor declaration.  ``==`` pins make
# the matrix reproducible; ``scripts/check_dependency_matrix.py`` enforces that
# the minimum file is pinned while the latest file remains a floor.

numpy==1.24.0
pandas==1.5.3
scipy==1.9.3
pytz==2023.3
packaging==26.3

# data-yahoo / data-cn transitives: keep a working TLS stack.
pyOpenSSL==25.0.0
cryptography==45.0.0
