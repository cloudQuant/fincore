参考这个优化代码格式的脚本，为这个项目写一个优化代码格式的脚本 /Users/yunjinqi/Documents/source_code/backtrader/scripts/optimize_code.sh

## 已完成

已创建 `scripts/optimize_code.sh`，针对 fincore 项目做了以下适配：

- **pyupgrade**: `--py311-plus`（项目最低支持 Python 3.11）
- **ruff check --fix**: 自动修复 lint 问题（含 isort 导入排序、UP pyupgrade 规则等，配置来自 `pyproject.toml`）
- **ruff format**: 替代 black + isort，统一使用 ruff 格式化（line-length=120）
- **pip install -U .**: 重新安装验证
- **pytest tests -n 8**: 并行测试确认无破坏

运行方式：
```bash
bash scripts/optimize_code.sh
```