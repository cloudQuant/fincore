# Empyrical 已实现函数完整索引

**更新日期：** 2025-01-11
**版本：** v0.6.0
**总函数数：** 52+

---

## 📑 目录

- [收益指标](#收益指标)
- [风险调整收益指标](#风险调整收益指标)
- [风险指标](#风险指标)
- [回撤分析](#回撤分析)
- [统计特性](#统计特性)
- [相关性分析](#相关性分析)
- [连续涨跌统计](#连续涨跌统计)
- [年度指标](#年度指标)
- [市场关系](#市场关系)

---

## 收益指标

### `cum_returns(returns, starting_value=0)`
计算累计收益

### `cum_returns_final(returns, starting_value=0)`
计算最终累计收益

### `aggregate_returns(returns, convert_to)`
聚合收益到不同时间周期

### `annual_return(returns, period=DAILY, annualization=None)`
计算年化收益率

### `cagr(returns, period=DAILY, annualization=None)`
计算复合年增长率（等同于年化收益）

### `annual_return_by_year(returns, period=DAILY, annualization=None)`
计算每年的年化收益率

### `annual_active_return(returns, factor_returns, period=DAILY, annualization=None)`
计算年化主动收益（相对基准）

---

## 风险调整收益指标

### `sharpe_ratio(returns, risk_free=0.0, period=DAILY, annualization=None)`
计算夏普比率

### `sharpe_ratio_by_year(returns, risk_free=0.0, period=DAILY, annualization=None)`
计算每年的夏普比率

### `sortino_ratio(returns, required_return=0.0, period=DAILY, annualization=None)`
计算索提诺比率（使用下行风险）

### `calmar_ratio(returns, period=DAILY, annualization=None)`
计算卡玛比率（年化收益/最大回撤）

### `omega_ratio(returns, risk_free=0.0, required_return=0.0)`
计算Omega比率

### `information_ratio(returns, factor_returns, period=DAILY, annualization=None)`
计算信息比率

### `information_ratio_by_year(returns, factor_returns, period=DAILY, annualization=None)`
计算每年的信息比率

### `treynor_ratio(returns, factor_returns, risk_free=0.0, period=DAILY, annualization=None)`
计算特雷诺比率（收益/Beta）

### `m_squared(returns, factor_returns, risk_free=0.0, period=DAILY, annualization=None)`
计算M平方指标（Modigliani-Modigliani measure）

### `sterling_ratio(returns, risk_free=0.0, period=DAILY, annualization=None)` ⭐新增
计算斯特林比率（使用平均回撤）

### `burke_ratio(returns, risk_free=0.0, period=DAILY, annualization=None)` ⭐新增
计算Burke比率（使用回撤平方和的平方根）

### `kappa_three_ratio(returns, risk_free=0.0, period=DAILY, annualization=None, mar=0.0)` ⭐新增
计算Kappa 3比率（使用三阶下偏矩）

### `adjusted_sharpe_ratio(returns, risk_free=0.0, period=DAILY, annualization=None)` ⭐新增
计算调整夏普比率（考虑偏度和峰度）

### `stutzer_index(returns, risk_free=0.0, target_return=0.0)` ⭐新增
计算Stutzer指数（信息论风险测度）

---

## 风险指标

### `annual_volatility(returns, period=DAILY, annualization=None)`
计算年化波动率

### `annual_volatility_by_year(returns, period=DAILY, annualization=None)`
计算每年的年化波动率

### `downside_risk(returns, required_return=0.0, period=DAILY, annualization=None)`
计算下行风险

### `tracking_error(returns, factor_returns, period=DAILY, annualization=None)`
计算跟踪误差

### `annual_active_risk(returns, factor_returns, period=DAILY, annualization=None)`
计算年化主动风险

### `tracking_difference(returns, factor_returns)`
计算跟踪偏离度

### `residual_risk(returns, factor_returns)` ⭐新增
计算残差风险（不可分散风险）

---

## 回撤分析

### `max_drawdown(returns)`
计算最大回撤

### `max_drawdown_by_year(returns)`
计算每年的最大回撤

### `max_drawdown_days(returns)`
计算最大回撤持续天数

### `max_drawdown_weeks(returns)`
计算最大回撤持续周数

### `max_drawdown_months(returns)`
计算最大回撤持续月数

### `max_drawdown_recovery_days(returns)`
计算最大回撤修复天数

### `max_drawdown_recovery_weeks(returns)`
计算最大回撤修复周数

### `max_drawdown_recovery_months(returns)`
计算最大回撤修复月数

### `second_max_drawdown(returns)`
计算第二大回撤

### `second_max_drawdown_days(returns)` ⭐新增
计算第二大回撤持续天数

### `second_max_drawdown_recovery_days(returns)` ⭐新增
计算第二大回撤修复天数

### `third_max_drawdown(returns)`
计算第三大回撤

### `third_max_drawdown_days(returns)` ⭐新增
计算第三大回撤持续天数

### `third_max_drawdown_recovery_days(returns)` ⭐新增
计算第三大回撤修复天数

### `get_max_drawdown_period(returns)`
获取最大回撤的起止日期

---

## 统计特性

### `skewness(returns)` ⭐新增
计算收益分布的偏度

### `kurtosis(returns)` ⭐新增
计算收益分布的峰度

### `hurst_exponent(returns)` ⭐新增
计算赫斯特指数（长期记忆性）

### `tail_ratio(returns)`
计算尾部比率

### `value_at_risk(returns, cutoff=0.05)`
计算在险价值（VaR）

### `conditional_value_at_risk(returns, cutoff=0.05)`
计算条件在险价值（CVaR）

---

## 相关性分析

### `stock_market_correlation(returns, stock_market_returns)` ⭐新增
计算与股市的相关性

### `bond_market_correlation(returns, bond_market_returns)` ⭐新增
计算与债市的相关性

### `futures_market_correlation(returns, futures_market_returns)` ⭐新增
计算与期市的相关性

### `serial_correlation(returns, lag=1)` ⭐新增
计算序列相关性（自相关）

### `stability_of_timeseries(returns)`
计算时间序列的稳定性（R²）

---

## 连续涨跌统计

### 连续天数/周数/月数

#### `max_consecutive_up_days(returns)` ⭐新增
计算最大连涨天数

#### `max_consecutive_down_days(returns)` ⭐新增
计算最大连跌天数

#### `max_consecutive_up_weeks(returns)` ⭐新增
计算最大连涨周数

#### `max_consecutive_down_weeks(returns)` ⭐新增
计算最大连跌周数

#### `max_consecutive_up_months(returns)` ⭐新增
计算最大连涨月数

#### `max_consecutive_down_months(returns)` ⭐新增
计算最大连跌月数

### 涨跌幅度

#### `max_consecutive_gain(returns)` ⭐新增
计算最大连续涨幅

#### `max_consecutive_loss(returns)` ⭐新增
计算最大连续跌幅

#### `max_single_day_gain(returns)` ⭐新增
计算最大单日涨幅

#### `max_single_day_loss(returns)` ⭐新增
计算最大单日跌幅

### 日期信息

#### `max_consecutive_up_start_date(returns)` ⭐新增
获取最大连涨区间开始日期

#### `max_consecutive_up_end_date(returns)` ⭐新增
获取最大连涨区间结束日期

#### `max_consecutive_down_start_date(returns)` ⭐新增
获取最大连跌区间开始日期

#### `max_consecutive_down_end_date(returns)` ⭐新增
获取最大连跌区间结束日期

#### `max_single_day_gain_date(returns)` ⭐新增
获取最大单日涨幅日期

#### `max_single_day_loss_date(returns)` ⭐新增
获取最大单日跌幅日期

---

## 年度指标

### `annual_alpha(returns, factor_returns, risk_free=0.0)` ⭐新增
计算每年的Alpha

### `annual_beta(returns, factor_returns, risk_free=0.0)` ⭐新增
计算每年的Beta

---

## 市场关系

### `alpha(returns, factor_returns, risk_free=0.0, period=DAILY, annualization=None)`
计算Alpha（Jensen's Alpha）

### `beta(returns, factor_returns, risk_free=0.0)`
计算Beta

### `alpha_beta(returns, factor_returns, risk_free=0.0, period=DAILY, annualization=None)`
同时计算Alpha和Beta

### `up_capture(returns, factor_returns, period=DAILY, annualization=None)`
计算上行捕获率

### `down_capture(returns, factor_returns, period=DAILY, annualization=None)`
计算下行捕获率

### `up_down_capture(returns, factor_returns, period=DAILY, annualization=None)`
同时计算上行和下行捕获率

### `capture(returns, factor_returns, period=DAILY, annualization=None)`
计算捕获率

---

## 盈亏统计

### `win_rate(returns)`
计算盈利天数占比

### `loss_rate(returns)`
计算亏损天数占比

---

## 滚动指标

大多数指标都有对应的滚动版本，使用 `roll_` 前缀：

- `roll_sharpe_ratio()`
- `roll_sortino_ratio()`
- `roll_max_drawdown()`
- `roll_annual_volatility()`
- `roll_alpha()` / `roll_beta()`
- `roll_up_capture()` / `roll_down_capture()`
- `roll_tracking_error()`
- `roll_treynor_ratio()`
- `roll_m_squared()`
- `roll_annual_active_risk()`
- `roll_annual_active_return()`
- 等等...

---

## 🔍 使用示例

### 基本用法

```python
import empyrical
import pandas as pd

# 准备数据
returns = pd.Series([0.01, 0.02, -0.01, 0.03, -0.02])
benchmark = pd.Series([0.008, 0.018, -0.008, 0.025, -0.015])

# 计算各种指标
sharpe = empyrical.sharpe_ratio(returns)
sortino = empyrical.sortino_ratio(returns)
max_dd = empyrical.max_drawdown(returns)
alpha = empyrical.alpha(returns, benchmark)
beta = empyrical.beta(returns, benchmark)
```

### 高级用法

```python
# 年度指标
annual_sharpe = empyrical.sharpe_ratio_by_year(returns)
annual_alpha = empyrical.annual_alpha(returns, benchmark)
annual_beta = empyrical.annual_beta(returns, benchmark)

# 连续涨跌分析
max_up_days = empyrical.max_consecutive_up_days(returns)
max_gain = empyrical.max_consecutive_gain(returns)
gain_date = empyrical.max_single_day_gain_date(returns)

# 统计特性
skew = empyrical.skewness(returns)
kurt = empyrical.kurtosis(returns)
hurst = empyrical.hurst_exponent(returns)

# 相关性分析
stock_corr = empyrical.stock_market_correlation(returns, stock_index)
serial_corr = empyrical.serial_correlation(returns, lag=1)

# 高级风险指标
sterling = empyrical.sterling_ratio(returns)
burke = empyrical.burke_ratio(returns)
kappa3 = empyrical.kappa_three_ratio(returns)
adj_sharpe = empyrical.adjusted_sharpe_ratio(returns)
stutzer = empyrical.stutzer_index(returns)
residual = empyrical.residual_risk(returns, benchmark)
```

---

## 📚 参数说明

### 常用参数

- `returns`: 策略收益序列（非累计）
- `factor_returns`: 基准/市场收益序列（非累计）
- `risk_free`: 无风险收益率（默认0.0）
- `period`: 收益周期（DAILY, WEEKLY, MONTHLY等）
- `annualization`: 年化因子（可选，会覆盖period）
- `required_return`: 最小可接受收益（用于Sortino等）
- `cutoff`: 分位数截断点（用于VaR/CVaR）

### 返回值类型

- 单一指标：`float`
- 年度指标：`pd.Series` (年份为索引)
- 日期函数：`pd.Timestamp` 或 `None`
- 天数/周数/月数：`int` 或 `np.nan`

---

## ⚠️ 注意事项

1. **数据格式**
   - 收益应该是非累计收益（simple returns）
   - 支持 `pd.Series` 和 `np.ndarray` 输入
   - 日期索引对某些函数是必需的

2. **边界情况**
   - 空数据通常返回 `np.nan`
   - 数据不足时会返回 `np.nan`
   - 某些函数有最小数据量要求

3. **性能考虑**
   - 滚动指标计算量较大
   - 长时间序列建议使用优化的算法
   - Hurst指数和Stutzer指数计算较慢

4. **依赖项**
   - numpy >= 1.17.0
   - pandas >= 0.25.0
   - scipy >= 1.3.0
   - six >= 1.10

---

## 📖 更多信息

- **项目主页**: https://github.com/quantopian/empyrical
- **文档**: https://empyrical.ml4trading.io/
- **问题反馈**: GitHub Issues
- **贡献指南**: CONTRIBUTING.md

---

**索引版本：** v1.0
**最后更新：** 2025-01-11
**维护者：** Empyrical Contributors
