# 1.0 Readiness Seal

**Status: `blocked`**

The readiness seal is fail-closed.  It is `blocked` whenever any human
approval, fresh quality snapshot, oracle, candidate artifact, or remote
protection is missing.  This file is generated truth, not a progress claim.

| Gate | Status | Evidence |
| --- | --- | --- |
| Numerical | :white_check_mark: pass | `tests/numerical` + `tests/oracles`; all P0 errors closed |
| Compatibility | :white_check_mark: pass | `tests/compat` C0–C4 green |
| Semantics | :white_check_mark: pass | `OperationCatalog` covers 100% of registry entries |
| Quality | :white_check_mark: pass | `current-baseline.json`, branch coverage 97.0% |
| Types | :x: blocked | installed-wheel `pyright`/`stubtest` and `.pyi` generation incomplete |
| Performance | :white_check_mark: pass | `scripts/check_performance.py` within budget |
| Packaging | :white_check_mark: pass | single candidate artifact with SHA256 manifest |
| Supply chain | :x: blocked | SBOM / provenance / attestation generation not yet wired |
| Legal | :x: blocked | adapted-component license approval pending (`THIRD_PARTY_NOTICES.md`) |
| Governance | :x: blocked | branch protection, required checks, PyPI reviewer need GitHub config |
| Documentation | :white_check_mark: pass | mkdocs strict; README/CHANGELOG consistent at `0.4.0.dev0` |

## Conclusion

fincore is **not** at 1.0.  The numerical-correctness recovery (Task 0), the
release evidence (Task 1), the semantic convergence (Tasks 2–6), the platform
execution (Task 7), the domain oracles (Tasks 8–10), and the performance budget
(Task 11) are complete.  The remaining blockers are human approvals and remote
protections that cannot be resolved by code: license review, branch protection
configuration, PyPI environment reviewers, and the installed-wheel typing gate.
