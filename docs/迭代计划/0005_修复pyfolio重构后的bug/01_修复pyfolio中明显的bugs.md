### 背景

pyfolio刚进行重构，把计算类的函数拆分到了Empyrical类中，Pyfolio这个类继承Empyrical类，只负责画图展示。

### 问题

1. 现在有很多格式上的问题，不符合pep8规范的，需要修复一下。
2. 各个函数的参数，中间使用的变量等可能不太符合规范，需要修复一下。
3. 中间可能存在一些bug，需要修复一下。

### 限制

1. 不允许修改里面函数的逻辑，只允许修改格式和变量名。
2. 不允许修改里面函数的参数，只允许修改格式和变量名。
3. 不允许修改里面函数的返回值，只允许修改格式和变量名。