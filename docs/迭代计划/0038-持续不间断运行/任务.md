### 背景

现在在项目中，使用claude dangerously-skip-permissions 打开claude cli, 然后输入docs/0037-不断修复bug并完善项目/readme.md这个之后，点击enter之后，就会运行这个readme.md中的任务。但是这个任务结束之后，并不能继续运行这个任务，我现在希望能够继续运行这个任务，直到这个任务能够全部完成。

### 优化脚本

之前写了一个脚本：scripts/claude_night_runner.sh

但是这个脚本运行的时候，跟直接使用背景中的使用方法运行结果不太一样，检查原因，并修复。希望能够实现这种效果，在每次任务结束之后，考虑使用/compact进行压缩，然后继续运行readme.md这个里面的任务，把原先在claude cli中输出的内容，输出到日志中。

根据这个目标，优化claude_night_runner.sh这个脚本，实现这个效果；